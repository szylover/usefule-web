{
  "name": "useful-web",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/portal"],
  "scripts": {
    "dev": "pnpm --filter portal run dev",
    "build": "pnpm --filter portal run build",
    "tauri": "tauri",
    "tauri:dev": "npm run tauri -- dev",
    "tauri:build": "npm run tauri -- build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
